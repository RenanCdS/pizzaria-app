import {
    StyleSheet
} from 'react-native';

const styles = StyleSheet.create({
    defaultTextInput: {
        backgroundColor: 'rgba(100, 100, 100, .3)' 
    },
    defaultScreen: {
        padding: 10
    }
});

export default styles;